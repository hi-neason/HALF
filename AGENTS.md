# HALF 开发约定

## 项目与范围

- HALF 是使用 Java 开发的轻量级 harness agent 框架，当前阶段是 LLM 协议与模型抽象。
- 项目主要用于学习 Agent 生态技术原理；实现应清晰、可追踪、可测试，便于理解每个机制的工作过程。
- 本目录是主工程 Git 根目录；Git 检查、构建、测试和提交均以本目录为工作目录。
- 先阅读 `README.md` 和 `docs/architecture.md`。提案与已实现能力必须明确区分。

## 实现原则

- 先学习并实现 LLM 协议与模型抽象，再实现工具调用和单 Agent 执行循环；不提前搭建多模块或多 Agent 平台。
- AI 相关机制使用原生 Java 自行实现，不引入 AI 框架、Agent 框架或模型供应商 SDK 来代替实现。
- 模型适配器通过 HTTP 调用模型服务，自行实现请求与响应映射；核心循环与供应商协议、UI 和存储解耦。
- 工具由宿主显式注册；模型输出的工具调用必须校验，不能直接当作可信的执行指令。
- 明确定义停止原因、工具错误和调用结果关联；避免没有上限的循环。
- 引入第三方代码时遵守其许可证，保留要求的版权及归属声明。
- 项目文档聚焦自身功能、接口和设计；个人研究笔记与本机工作区信息不纳入版本控制。
- 用户已允许按需使用 Jackson、HTTP 客户端等通用基础库，无需因这类依赖本身重复申请许可；优先使用 JDK 标准库，避免重型依赖，并说明新增依赖的用途。
- 通用库只承担基础能力；模型协议、工具调用、上下文管理，以及后续的记忆、检索增强和多 Agent 编排等 AI 机制由项目代码实现。

## 验证与交付

- 使用 Java 21+ 与 Maven；`mvn test` 验证行为，`mvn package` 验证构建，真实模型示例见 README。
- 核心行为测试使用可控的假模型与假工具，不依赖 API 密钥、外网或付费模型。
- 变更保持小而可审查，不覆盖用户已有改动。
- 完成时说明修改内容、已运行的验证及尚未实现的能力；不要把设计文档描述为已完成实现。
